name: keep-alive

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping bot on Render
        run: curl -fsS --max-time 60 https://nashdom-bot.onrender.com/ || true
